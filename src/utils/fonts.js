import { Dancing_Script, Oswald, Space_Mono } from 'next/font/google';

export const dancingScript = Dancing_Script({ subsets: ['latin'] });
export const oswald = Oswald({ subsets: ['latin'] });
export const spaceMono = Space_Mono({ subsets: ['latin'], weight: ['400', '700'],});